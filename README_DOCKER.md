# 🐳 InventoryPro - Docker Setup

Все сервисы работают через Docker Compose.

## Быстрый старт

1. **Запустите Docker Desktop**

2. **Запустите все сервисы:**
```bash
./docker-start.sh
```

Или вручную:
```bash
docker-compose up -d --build
```

3. **Создайте миграции и начальные данные** (см. DOCKER_START.md)

4. **Откройте приложение:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

**Логин:** admin  
**Пароль:** admin123

## Структура Docker

- **postgres** - PostgreSQL 15 база данных
- **backend** - FastAPI приложение
- **frontend** - React + Vite приложение

Все сервисы связаны через Docker network и используют внутренние имена хостов.

## Production Deployment

Для production используйте тот же `docker-compose.yml` с правильными настройками в `.env` файле.

Подробные инструкции: см. DOCKER_START.md


