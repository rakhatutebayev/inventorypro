# 🚀 Как запустить Frontend

Backend уже работает на `http://localhost:8000`. Теперь нужно запустить frontend.

## Шаги для запуска:

### 1. Откройте терминал и перейдите в папку проекта:
```bash
cd /Users/rakhat/Documents/webhosting/InventoryPro/frontend
```

### 2. Установите зависимости (если еще не установлены):
```bash
npm install
```

### 3. Запустите frontend:
```bash
npm run dev
```

### 4. Откройте браузер:
После запуска вы увидите сообщение типа:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

Откройте http://localhost:3000 в браузере.

## Данные для входа:

- **URL:** http://localhost:3000
- **Логин:** `admin`
- **Пароль:** `admin123`

## Если что-то не работает:

1. **Убедитесь, что backend работает:**
   - Проверьте: http://localhost:8000/health
   - Должен вернуться: `{"status":"ok"}`

2. **Проверьте, что порт 3000 свободен:**
   ```bash
   lsof -i :3000
   ```
   Если порт занят, остановите процесс или измените порт в `vite.config.ts`

3. **Очистите кеш и переустановите зависимости:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```


