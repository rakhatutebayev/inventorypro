# InventoryPro Mobile App

Мобильное приложение для управления IT оборудованием на React Native/Expo.

## 🚀 Запуск

```bash
cd mobile
npm install
npm start
```

Затем:
- Нажмите `i` для iOS симулятора
- Нажмите `a` для Android эмулятора  
- Отсканируйте QR код в приложении Expo Go на телефоне

## ✨ Возможности

### ✅ Реализовано:

1. **Аутентификация**
   - Логин/Logout
   - JWT токены
   - Сохранение сессии

2. **Настройки API**
   - Настройка Base URL (HTTPS) или хоста/порта
   - Сохранение конфигурации

3. **Assets (Активы)**
   - Список активов
   - Поиск по inventory number, serial, vendor, model
   - Сканирование QR/баркодов для поиска
   - Отображение информации об активе

4. **Movements (Перемещения)**
   - Сканирование актива для перемещения
   - Выбор назначения (Warehouse/Employee)
   - Выполнение перемещения

5. **Inventory (Инвентаризация)**
   - Создание сессий инвентаризации
   - Сканирование активов для проверки
   - Завершение сессий
   - Просмотр истории сессий

6. **QR/Barcode Scanner**
   - Сканирование QR кодов
   - Сканирование баркодов (EAN13, EAN8, Code128, Code39)
   - Удобный интерфейс с рамкой сканирования

## 📱 Структура

```
mobile/
├── src/
│   ├── components/       # Компоненты (Scanner)
│   ├── config/          # Конфигурация API
│   ├── screens/         # Экраны приложения
│   ├── services/        # API сервисы
│   ├── store/           # Zustand stores
│   └── types/           # TypeScript типы
├── App.tsx              # Главный компонент
└── app.json             # Конфигурация Expo
```

## 🔧 Технологии

- **React Native** с Expo
- **React Navigation** для навигации
- **Zustand** для state management
- **TanStack Query** для работы с API
- **Axios** для HTTP запросов
- **AsyncStorage** для локального хранения
- **Expo Camera** для сканирования QR/баркодов

## ⚙️ Конфигурация

По умолчанию API настроен на прод: `https://ams.it-uae.com/api/v1`.

Если нужно поменять:

1. Откройте Settings
2. Введите Base URL (например, `https://ams.it-uae.com/api/v1`) и сохраните
3. Или включите Advanced и задайте host/port/protocol

**Важно:** Убедитесь, что бэкенд доступен по указанному адресу и порту с вашего мобильного устройства.

## 📝 Примечания

- Приложение использует тот же API, что и веб-версия
- Все данные синхронизируются с сервером
- Требуется подключение к интернету для работы
- Камера требуется для сканирования QR/баркодов

